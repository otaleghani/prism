# Installing prism

- Get the iso or build it yourself with `nix build .#nixosConfigurations.iso.config.system.build.isoImage`
- Burn it in a USB
- Boot it
- Open gparted and partition your disk
- Run the installer
- Reboot
