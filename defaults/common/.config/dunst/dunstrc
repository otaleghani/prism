[global]
    # BEHAVIOR 
    history_length = 20
    
    # Only show 1 notification at a time to prevent stacking
    notification_limit = 1
    
    # Show how many are hidden "(1 more)"
    indicate_hidden = yes
    
    # Sort by urgency, then time
    sort = yes
    
    # Follow mouse
    follow = mouse
    
    # Geometry (Bottom Right)
    width = 350
    height = 300
    origin = bottom-right
    offset = 20x20
    
    # Icons
    icon_position = left
    max_icon_size = 60
    
    # --- STYLING ---
    transparency = 10
    padding = 15
    horizontal_padding = 15
    text_icon_padding = 15
    frame_width = 2
    gap_size = 8
    
    font = JetBrainsMono Nerd Font 10
    line_height = 4
    markup = full
    
    # Actions
    mouse_left_click = do_action, close_current
    mouse_middle_click = close_all
    mouse_right_click = close_current
