# btop configuration

# Use the dynamic Prism theme
color_theme = "prism"

# Theme background: False = transparent (uses terminal bg)
theme_background = False

# Set to True to enable "h,j,k,l" keys for moving
vim_keys = True

# Graph settings
rounded_corners = True
graph_symbol = "braille"
shown_boxes = "cpu mem net proc"
update_ms = 2000
